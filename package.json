{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared",
    "electron-client",
    "sample-electron-app"
  ],
  "husky": {
    "hooks": {
      "post-checkout": "cd shared && yarn run build",
      "pre-commit": "cd shared && yarn run build"
    }
  },
  "devDependencies": {
    "husky": "^1.2.0"
  }
}
