{
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared",
    "electron-client",
    "sample-electron-app"
  ],
  "husky": {
    "hooks": {
      "post-checkout": "cd shared && yarn run build",
      "pre-commit": "cd shared && yarn run build"
    }
  },
  "devDependencies": {
    "husky": "^1.2.0",
    "tslint": "^5.12.1",
    "tslint-eslint-rules": "^5.4.0",
    "tslint-lines-between-class-members": "^1.3.1",
    "tslint-react": "^3.6.0"
  }
}
